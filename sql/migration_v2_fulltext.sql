-- ============================================================
-- V2 迁移脚本：添加 FULLTEXT 索引以支持关键词检索
-- ============================================================

ALTER TABLE `document_chunk`
    ADD FULLTEXT KEY `ft_chunk_content` (`content`);
